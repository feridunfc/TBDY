# _archive

This folder is the quarantine marker for preserved but inactive drift/platform material.

Nothing is deleted in this reset task.

Material to keep inactive for `BEAM_RUNTIME_CLOSURE` is inventoried in `BEAM_CLOSURE_REPO_INVENTORY.md`, including:

- `tbdy_engine/archx/**`
- workbench bundle code
- ARCH-X runner / CLI / serializer / markdown report
- demo snapshot runner paths
- coverage/distribution/cache analytics surfaces
- execution history persistence
- report contract metadata
- generated/backup contract artifacts
- audit/governance/runtime platform layers

Archive means not imported by the active beam runtime, not used by `runner_v2`, not used by reports, and not used by minimal beam tests.
