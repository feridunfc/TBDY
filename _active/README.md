# _active

This folder is the branch-equivalent active workspace marker for `REPO_RESET_FOR_BEAM_RUNTIME_CLOSURE`.

No runtime behavior is changed in this reset task.

Target active runtime for the next sprint:

```text
ETABS tables
→ Beam normalizer
→ BeamEvaluationPackage
→ CheckAdapter
→ CheckResult[]
→ ReportingFacade.generate(check_results)
→ engine_report.json
→ engine_report.xlsx
```

Active material identified for the minimal boring beam runtime is listed in `BEAM_CLOSURE_REPO_INVENTORY.md`.

Do not import archived ARCH-X/platformization paths from the active beam closure runtime.
